export const metadata = {
  title: "Visual Analytics - Quality Pulse",
  description: "Advanced multi-view data explorer.",
};

export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
